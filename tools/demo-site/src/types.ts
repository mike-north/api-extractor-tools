/**
 * Available policy names in the demo site.
 * Note: 'custom' policy is not currently supported in the new AST-based API.
 */
export type PolicyName = 'default' | 'read-only' | 'write-only' | 'custom'
