#!/bin/sh
set -e

# Fix all issues (lint, format, markdown)
pnpm fix

# Build all packages
pnpm build

# Run tests for uncommitted changes
pnpm test:uncommitted

# Generate API reports
pnpm generate:api-report

# Generate documentation
pnpm generate:docs
