# Enable symlink following for better compatibility with tools that analyze node_modules
symlink=true
# Hoist workspace dependencies to make them easier to resolve
hoist=true
# Hoist all patterns
hoist-pattern[]=*
